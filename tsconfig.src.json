{
	"extends": "./tsconfig.base.json",
	"include": ["${configDir}/src"],
	"compilerOptions": {
		"noEmit": false,
		"rootDir": "${configDir}/src",
		"outDir": "${configDir}/lib",
		"tsBuildInfoFile": "${configDir}/build/tsconfig.build.tsbuildinfo.json"
	}
}
